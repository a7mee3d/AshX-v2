using System;

namespace AshX.Core
{
    public static class WindowManagerStatic
    {
        public static IntPtr MainHandle = IntPtr.Zero;
        public static IntPtr SecondHandle = IntPtr.Zero;
    }
}
